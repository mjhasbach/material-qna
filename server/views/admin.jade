block admin
    div.admin(
    ng-if='user.data && user.data.isAdmin',
    ng-show='view.current === "admin"',
    ng-controller='adminController',
    layout='row',
    flex
    )
        include admin_sidenav
        include admin_users
        include admin_qna
        include admin_history